package com.fiap.comunidadeconectada.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de Cores - Comunidade Conectada

// Cores Primárias
val PrimaryBlue = Color(0xFF0a7ea4)
val PrimaryBlueDark = Color(0xFF084d6e)
val PrimaryBlueLight = Color(0xFF1a9ec8)

// Cores Secundárias
val SecondaryGreen = Color(0xFF22C55E)
val SecondaryGreenDark = Color(0xFF16a34a)
val SecondaryGreenLight = Color(0xFF4ade80)

// Cores de Fundo
val BackgroundLight = Color(0xFFffffff)
val BackgroundDark = Color(0xFF151718)

// Cores de Texto
val TextLight = Color(0xFF11181C)
val TextDark = Color(0xFFECEDEE)
val TextSecondary = Color(0xFF6B7280)

// Cores de Destaque
val HighlightAmber = Color(0xFFF59E0B)
val HighlightRed = Color(0xFFEF4444)
val HighlightOrange = Color(0xFFFF6B35)

// Cores Neutras
val NeutralGray100 = Color(0xFFF3F4F6)
val NeutralGray200 = Color(0xFFE5E7EB)
val NeutralGray300 = Color(0xFFD1D5DB)
val NeutralGray400 = Color(0xFF9CA3AF)
val NeutralGray500 = Color(0xFF6B7280)

// Cores de Status
val SuccessGreen = Color(0xFF10B981)
val ErrorRed = Color(0xFFEF4444)
val WarningYellow = Color(0xFFFCD34D)
val InfoBlue = Color(0xFF3B82F6)

// Cores para Reputação
val ReputationExcellent = Color(0xFF10B981)  // Verde
val ReputationGood = Color(0xFF3B82F6)       // Azul
val ReputationNeutral = Color(0xFF6B7280)    // Cinza
val ReputationPoor = Color(0xFFF59E0B)       // Âmbar
val ReputationBad = Color(0xFFEF4444)        // Vermelho
